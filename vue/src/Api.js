var root = 'http://sym-vue-members:8888/'
var api = root + 'api/'

export default {
    root: root,
    memberSection: 1,
    // api endpoints
    member: api + 'member/',
    access: api + 'access/'
}
