var root = 'http://symphony-members-vue:8888/'
var api = root + 'api/'

export default {
    root: root,
    memberSection: 1,
    // api endpoints
    member: api + 'member/',
    access: api + 'access/'
}
